# VPS 部署指南 — bank2excel-h5 私有转换服务

把转换服务放到 VPS，手机/电脑随时随地用，**不再受 17MB 引擎下载、内网穿透、电脑开机的限制**。整站私有，不考虑数据隐私场景。

---

## 一、买哪台（2026-08 行情）

### 🥇 最便宜推荐：腾讯云轻量应用服务器
| 方案 | 配置 | 价格 | 备注 |
|------|------|------|------|
| **秒杀**（每日 10:00/15:00 两场） | 4核4G / 40G SSD / 300G流量 | **38元/年** | 新人首单，要抢 |
| **新人首单** | 2核2G / 40G SSD / 200G流量 | **68元/年** | 稳定入手，够用 |
| 同价续费 | 2核2G / 4M / 300G流量 | 99元/年 | 续费不涨价 |
| 学生认证 | 2核2G / 4M | 25.92元/3月 | 校园计划 |

入口：腾讯云官网 → 轻量应用服务器 → 新用户专区（每日秒杀 10:00/15:00）。

> ⚠️ **用 IP 访问无需备案**！本方案直接 `http://服务器IP` 访问，跳过 ICP 备案。
> 建议买 **北京/上海/广州** 地域（离你近延迟低）；选 **Ubuntu 22.04 LTS** 镜像。

### 其他选项
- 阿里云轻量：类似价位（新人 2核2G 约 99元/年）
- 免费：Oracle Cloud ARM 永久免费（注册门槛高）/ 腾讯云新用户免费试用 1 个月
- 不想买服务器：继续用"本地服务+cpolar 穿透"（零成本，电脑开机即可）

---

## 二、部署（约 10 分钟）

### 1. 登录服务器
```bash
ssh root@<服务器IP>     # 密码在腾讯云控制台设置; 或用密钥登录
```

### 2. 安装 Docker（Ubuntu 22.04 一条命令）
```bash
curl -fsSL https://get.docker.com | sh
systemctl enable docker && systemctl start docker
```

### 3. 拉取代码并构建
```bash
cd /opt
git clone https://github.com/jxuzhi-lab/bank2excel-h5.git
cd bank2excel-h5
docker compose up -d --build
```

### 4. 放行端口
腾讯云控制台 → 轻量服务器 → 防火墙 → **放行 80 端口**（入站，来源 0.0.0.0/0）。

### 5. 验证
```bash
curl http://<服务器IP>/          # 应返回手机上传页
```
手机浏览器打开 `http://<服务器IP>` → 选 PDF → 转换 → 下载 xlsx。

---

## 三、日常维护

| 操作 | 命令 |
|------|------|
| 看日志 | `docker logs -f bank2excel` |
| 重启 | `docker restart bank2excel` |
| 升级（拉新代码后） | `cd /opt/bank2excel-h5 && git pull && docker compose up -d --build` |
| 停止 | `docker compose down` |

- 磁盘：镜像约 1GB，40G SSD 绰绰有余
- 内存：转换是短时任务，2G 内存跑 200MB 内 PDF 无压力
- 流量：每次转换上传/下载各几 MB，200G/月流量完全够私人使用（几百次/月）

---

## 四、可选进阶

- **绑定域名**（可选）：域名需 ICP 备案才能用国内服务器绑域名；不备案就继续用 IP 访问
- **HTTPS**：IP 访问无法申请免费证书，如要 HTTPS 需域名+备案，或用 Cloudflare Tunnel（免费，绕开备案）
- **多用户**：当前是单用户私有；要分享给同事就加个简单 token 校验（改 server.py 一个装饰器）
- **备份**：无数据库无状态，代码在 GitHub，服务器随便重装
